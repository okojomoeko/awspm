#![warn(missing_docs)]
//! # AWS Profile Manager (awspm)
//!
//! A powerful CLI tool to manage AWS profiles with tags, aliases, and smart context switching.

/// Core shared logic and types (Domain & Generic Infra).
pub mod core;
/// Vertical slices for each feature (Command).
/// Vertical slices for each feature (Command).
pub mod features;

// Legacy modules (Removed)
